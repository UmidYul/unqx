# /
Бизнес-улучшения:

В блоке Следующий дроп добавить CTA “Забронировать уведомление в 1 клик” с prefilled slug из hero-поля для роста конверсии в дроп.
В калькуляторе после расчёта показывать “похожие свободные” прямо рядом с ценой (сейчас это только в checker), чтобы сократить путь до заявки.
В sticky flash-sale баннер добавить micro-copy “осталось N мест/slug” (если есть данные) для усиления urgency.
Системные улучшения:

Вынести общие icon-snippets в единый helper/partial, чтобы не дублировать SVG в шаблонах и JS.
Добавить unit-тесты на клиентские helper-функции public-home-search.js (normalize/calc/motion) через выделение их в тестируемый модуль.
Добавить lightweight visual regression для / (hero + modal open state + calculator result state), чтобы не терять UI-качество при следующих правках.

# /:slug
Бизнес-улучшения:

На slug-state (занят/pending) добавить CTA “Подобрать похожий свободный UNQ” с автоподбором и мгновенным открытием заявки.
На публичной карточке добавить “Создать такой же UNQ” (prefill паттерна) для конверсии с просмотра чужой визитки в заказ.
Для top badge (Топ недели) добавить share-snippet “Я в топе UNQ+” с параметрами кампании.
Системные улучшения:

Вынести текстовые статусы share/copy/save в единый i18n-слой (сейчас строки в public-card.js).
Разделить card-view.js на pure renderer + interaction module для более глубоких unit-тестов.
Добавить e2e-smoke на /:slug для сценариев: share fallback, save .vcf, avatar fallback, data-order-link на state-экранах.

# /profile
Бизнес-улучшения:

Во вкладке МОИ UNQ добавить быстрый CTA “Скопировать ссылку” у каждого slug для ускорения распространения.
Во вкладке ЗАЯВКИ добавить “Следующий шаг” по статусу (оплата/ожидание/заполнить карточку), чтобы уменьшить отток после подачи.
Во вкладке РЕФЕРАЛЫ добавить видимый прогресс до следующей награды (progress bar по threshold), чтобы увеличить мотивацию приглашать.
Системные улучшения:

Вынести форматирование статусов slug/request в общий shared helper между API и фронтом, чтобы исключить рассинхрон.
Разбить public/js/profile.js на модули (tabs, card-editor, slugs, settings, modal) для упрощения тестирования и сопровождения.
Добавить unit-тесты на клиентскую логику лимитов (slug/button/tag) и на modal lifecycle (open/close/focus trap).

# /leaderboard

Бизнес-улучшения:

Добавить на карточках top-3 CTA “Занять похожий UNQ” (prefill похожего slug) для конверсии из рейтинга.
Добавить бейдж “+N мест за период” как отдельный social-proof триггер в карточке участника.
Добавить фильтр “только новые за период” для вовлечения новых пользователей в соревнование.
Системные улучшения:

Вынести форматирование delta (тон, цвет, текст) в helper, чтобы не дублировать в top/table.
Добавить серверный флаг “данные за период неполные” (если нет достаточного окна) для корректного UI-пояснения.
Добавить e2e-smoke на share-кнопку (clipboard success/fallback path).

# /drops
Бизнес-улучшения:

В карточке дропа показать “сколько подписалось” (waitlistCount) как social proof для повышения конверсии на подписку.
Добавить блок “прошлые успешные дропы” с sold-out временем для усиления FOMO.
В live-pool добавить быстрый фильтр по паттерну (AAA, 000, 123) для ускорения выбора slug.
Системные улучшения:

Оптимизировать refreshLivePools: сейчас запросы по карточкам последовательные; можно батчить или выполнять Promise.all.
Добавить дебаунс/lock на waitlist submit, чтобы исключить повторные клики до ответа.
Вынести toast/live-feedback в общий utility для страниц home/leaderboard/drops/profile.

# /404 /500

Бизнес-улучшения:

На 500 добавить UTM-safe ссылку в поддержку Telegram (если ошибка повторяется), чтобы не терять пользователя.
Для 404 можно добавить блок “Популярные разделы” (/, /drops, /leaderboard) для возврата в воронку.

Системные улучшения:

Логировать id ошибки и показывать его на 500 (например ERR-xxxx) для быстрой поддержки.
Добавить rate-limited client error beacon на 500 (только hash/path/ua), чтобы быстрее ловить массовые сбои.